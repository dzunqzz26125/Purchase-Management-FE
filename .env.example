# Backend API (Render production). Không dùng http://localhost:3000/api
VITE_API_URL=https://purchase-management-be.onrender.com/api

# Nếu vẫn thấy request tới localhost:3000, xóa file .env.local (nếu có) và khởi động lại: npm run dev
